/** @type {import('next').NextConfig} */
const nextConfig = { reactStrictMode: false, images: {
    domains: ["wallpaperaccess.com", "github-readme-stats.vercel.app"]
} };

export default nextConfig;
